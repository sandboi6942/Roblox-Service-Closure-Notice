var trauma = window.prompt("Warning - the following email may contain sensitive and traumatising information. Would you like to proceed? (Yes/no)")
if (trauma == yes || trauma == Yes || trauma == YES) {
    alert("Alright then...")
} else {
    alert("Don't worry")
    alert("If you haven't gotten it by now...")
    alert("Your choices don't matter")
}